# PDF text extraction
pypdf==5.1.0

# Image OCR (Tesseract wrapper + image loading)
pytesseract==0.3.13
Pillow==11.1.0

# AWS SDK — no longer pre-installed since we use python:3.12-slim.
boto3==1.35.99

# Lambda runtime interface client (replaces the one built into the AWS base image).
awslambdaric==3.0.0
